#include "monty.h"
/**
  *for_nop- null function.
  *@head: Head of the stack.
  *@counter: line_number.

  *Return: no return
 */
void for_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
